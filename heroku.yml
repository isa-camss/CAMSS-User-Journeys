build:
  docker:
    web: Dockerfile.jupyter
    run:                               
   web: jupyter notebook --ip=0.0.0.0 --port=$PORT --allow-root --NotebookApp.token="$JUPYTER_TOKEN" --no-browser
    # worker: Dockerfile.flask
